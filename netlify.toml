[build]
  publish = "public"
  # This generates the config.js for the browser during every Netlify deployment
  command = "echo \"window.ENV_CONFIG = { SUPABASE_URL: '$SUPABASE_URL', SUPABASE_ANON_KEY: '$SUPABASE_ANON_KEY' };\" > public/config.js"
